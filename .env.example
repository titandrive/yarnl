# Yarnl Configuration
# Copy this file to .env and customize as needed
# All values shown are defaults — only set what you want to change

# Database
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=yarnl
POSTGRES_USER=yarnl
POSTGRES_PASSWORD=yarnl

# Admin Account
# Username for the initial admin user (created on first run)
ADMIN_USERNAME=admin
# Leave empty for passwordless login, or set a password
ADMIN_PASSWORD=

# App
# Port to expose on the host
PORT=3000
# Timezone for scheduled backups (e.g., America/New_York, Europe/London)
TZ=UTC

# Backups
# Override the default backup location (default: ./users/<username>/backups)
# Backups will be stored at: <BACKUP_PATH>/yarnl-backups/<username>/
# BACKUP_PATH=/mnt/user/drive

# OIDC / SSO (Optional)
# Set all three required values to enable OIDC via environment variables.
# When set, these override any OIDC settings configured in the admin UI.
# OIDC_ISSUER=https://auth.example.com
# OIDC_CLIENT_ID=yarnl
# OIDC_CLIENT_SECRET=your-client-secret

# Optional OIDC settings
# OIDC_PROVIDER_NAME=My SSO Provider
# OIDC_ICON_URL=https://auth.example.com/icon.png
# OIDC_DISABLE_LOCAL_LOGIN=false
# OIDC_AUTO_CREATE_USERS=true
# OIDC_DEFAULT_ROLE=user

# SSO Recovery
# Set to true to force local login when OIDC/SSO is misconfigured
# FORCE_LOCAL_LOGIN=true
